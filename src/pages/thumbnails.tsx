import React, { useEffect, useRef } from "react";

export default function Thumbnails({ img, setImg }: { img: string[], setImg: React.Dispatch<React.SetStateAction<string[]>> }) {

  return(
  <>
  
  </>)

}